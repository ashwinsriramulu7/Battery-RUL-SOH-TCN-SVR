# Battery-RUL-SOH-TCN-SVR
Using a piecewise SVR model to predict phase-wise degradation of batteries. A TCN learns degradation patterns wrt n(cycles) and then the SVR is trained on the TCN's predictions.
